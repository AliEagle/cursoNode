const os = require('os');

//console.log(os.arch()); //x64
//console.log(os.platform()); //linux
//console.log(os.cpus()); //Muestra cada uno de los nucleos del cpu con sus detalles
//console.log(os.cpus().length); //16
//console.log(os.constants); //Muestra los errores y señales del sistema

/*const SIZE = 1024;
function kb(bytes) {
	return bytes / SIZE;
}
function mb(bytes) {
	return kb(bytes) / SIZE;
}
function gb(bytes) {
	return mb(bytes) / SIZE;
}
console.log(gb(os.totalmem()));
console.log(os.freemem());
console.log(kb(os.freemem()));
console.log(mb(os.freemem()));
console.log(gb(os.freemem()));
*/

//console.log(os.homedir()); // /home/alicp
//console.log(os.tmpdir()); // /tmp
//console.log(os.hostname()); //LAPTOP-C6NFKK2D
//console.log(os.networkInterfaces()); //Muestra todas las interfaces de red
